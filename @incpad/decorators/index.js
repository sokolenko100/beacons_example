const aop = require('./decorators/aop');
const color = require('./decorators/color');
const need = require('./decorators/need');
const reactNativeSize = require('react-native-size');
module.exports = {
    aop,
    color,
    need,
    reactNativeSize,
};
